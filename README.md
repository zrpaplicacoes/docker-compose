## AWS ECS Rails Deploy

A Docker image that extends docker-compose to ease gitlab-ci pipelines to be deployed to AWS ECS.

It encapsulates the ecs-deploy script into the image, installs awscli, jq and bash. 
